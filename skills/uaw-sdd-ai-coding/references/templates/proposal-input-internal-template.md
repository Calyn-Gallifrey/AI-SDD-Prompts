# proposal-input.md Internal Template

This file is generated by `uaw-sdd-ai-coding` from `Brief Design（人工简要设计）`.

Developers do not manually fill this template.

## 1. Task Basic Information

SDD Version（SDD版本）：SDD2.0

Feature Workspace Root（功能资产根目录）：sdd2-features

Feature Name（功能名称）：

Feature Type（功能类型）：

Module（所属模块）：

Goal（一句话目标）：

## 2. Change Identification

Change Scope（变更范围）：

Forbidden Changes（禁止变更）：

Priority（优先级）：

Sprint（迭代）：

Output Directory（输出目录）：

## 3. Requirement Details

API（接口）：

Request Params（入参）：

Response Params（出参）：

Business Logic（业务逻辑）：

Related Impact（关联逻辑与影响面）：

Confirmed Facts（已确认信息）：

Assumptions（推断信息）：

Open Questions（待确认问题）：

## 4. Reference Basis

Reference Assets（参考资产）：

Reference Notes（参考说明）：

Rules:
- 仅记录当前 SDD2.0 资产、当前代码工程资产或用户明确指定的参考资料。
- 禁止默认引用旧版 SDD 目录或未被用户指定的历史路径。

## 5. SDD Execution Requirements

AI Knowledge Base（AI 知识底座）：Skill references

Code Baseline（代码基线）：current Git working tree

Required Flow（强制流程）：

```text
proposal-input.md → spec.md → design.md → tasks.md → implementation → code-review-findings.md → Auto-fix → Unit Test Summary → archive.md
```

## Process Status

Current Stage：

Stage Status：

Last Completed Step：

Next Required Step：

Blocked Reason：

## Process Audit Trail

| Time | Stage | Action | Result | Next Step |
|---|---|---|---|---|

# Proposal 输入

> 本文件是根据当前已确认 Brief Design 组装的 SDD2 内部资产。模板文字和示例永远不是需求。正文必须以简体中文为主体。

## 1. 基本信息

- Feature ID：
- 功能名称（Feature Name）：
- 功能类型（Feature Type）：`query` / `submit` / `edit` / `enhancement` / `refactor` / `fix`
- 所属模块（Module）：
- 迭代（Sprint）：
- 优先级（Priority）：`P0` / `P1` / `P2`
- 来源 Brief：`./brief-design.md`
- 来源 Brief SHA-256：
- 生成时间：

## 2. 目标与范围

### 一句话目标


### 已批准变更范围


### 禁止变更


### 非目标


## 3. 当前基线

| 来源 | 精确路径/符号 | 已观察事实 | 时效/Commit | 置信度 |
|---|---|---|---|---|
| 当前代码 |  |  |  | `confirmed` / `inferred` |
| 当前配置/Schema |  |  |  | `confirmed` / `inferred` |
| 当前用户消息 |  |  |  | `confirmed` |
| 明确指定的历史资产（如有） |  |  |  | `context-only` |

对于 `enhancement`、`refactor` 和 `fix`，描述既有行为和已确认增量。请求只是修改既有流程时，不得虚构新流程。

## 4. 功能输入

- 参与者/调用方：
- 触发方式/API：
- 输入：
- 输出：
- 业务规则：
- 错误行为：
- 安全/数据边界：
- 兼容性要求：

## 5. 工程上下文

- 候选模块/文件：
- 已识别框架/构建工具：
- 已路由后端规则：
- 已路由模型规则：
- 已路由 Transaction/上下文参考：
- 候选测试栈/配置：

## 6. 验收意图

| ID | 可观察验收结果 | 证据来源 |
|---|---|---|
| AC-01 |  |  |

## 7. 风险与待确认问题

| ID | 类型 | 问题/风险 | 未解决影响 | 所有者 | 必须在何时前解决 |
|---|---|---|---|---|---|
| Q-01 | `open-question` / `risk` / `assumption` |  |  |  | Spec / Design / Tasks |

不得把假设升级为已确认需求。不确定性会改变范围、行为、数据、安全或验收时，阻塞相应下一阶段。

## 8. 控制状态投影

- 状态权威文件：`./.sdd2/feature-state.json`
- Attempt：
- 控制修订：
- 当前阶段：`proposal-input`
- 阶段状态：
- 下一必需动作：
- 资产 SHA-256：

本节只投影控制状态，不能授权流程继续。
